version: "3.8"

services:
  deepseek-coder:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
#    runtime: nvidia
    ports:
      - "8000:8000"
    volumes:
      - ~/.cache/huggingface:/root/.cache/huggingface